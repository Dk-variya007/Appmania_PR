import 'package:flutter/material.dart';
import '../../route_utills.dart';

Widget productCard({
  required Size size,
  required Map<String, dynamic> product,
  required BuildContext context,
}) {
  return Stack(
    alignment: Alignment.topRight,
    children: [],
  );
}
